{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "The main window: core APIs only. http_get is an app command and needs no permission entry.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
